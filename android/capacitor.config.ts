import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.race.fanatic.app',
  appName: 'Race Fanatic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
